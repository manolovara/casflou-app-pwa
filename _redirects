# Minimal redirects - only handle SPA routing
# Don't use clean URLs until routing is stable

# Redirect root to index
/               /index.html          200

# Serve everything else as-is
/*              /:splat              200
